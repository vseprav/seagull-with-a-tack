# seagull-with-a-tack

Signal bot that sends CoT (Cursor on Target) messages to ATAK.

## Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install pytest
```

## Run tests

```bash
.venv/bin/pytest tests/ -v
```
